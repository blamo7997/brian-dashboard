<section class="bc-account-page">
  <div class="bc-account-card">
    <p class="bc-eyebrow">Brian & Co Account</p>
    <h1>Your private Brian & Co access begins here.</h1>
    <p class="bc-lead">
      Create or access your Brian & Co account for concierge guidance, role requests, complimentary-access review,
      digital-product access, saved work, and founder-reviewed pathways.
    </p>

    <div class="bc-actions">
      <a href="{{ routes.account_login_url }}">Sign in securely</a>
      <a href="{{ routes.account_register_url }}">Create account</a>
      <a href="{{ routes.root_url }}">Return home</a>
    </div>

    <div class="bc-note">
      Brian & Co keeps your experience branded and website-first wherever Shopify permits. Secure account authentication may be handled by Shopify behind the scenes, but your account pathway begins here.
    </div>
  </div>
</section>

<style>
.bc-account-page{min-height:72vh;padding:clamp(34px,6vw,86px) 18px;background:radial-gradient(circle at top left,#fff3df,#fff 42%,#f4eadf)}
.bc-account-card{max-width:860px;margin:0 auto;border:1px solid rgba(31,26,23,.13);border-radius:34px;padding:clamp(28px,5vw,56px);background:rgba(255,255,255,.78);box-shadow:0 28px 90px rgba(31,26,23,.10);color:#1f1a17}
.bc-eyebrow{letter-spacing:.18em;text-transform:uppercase;font-size:12px;opacity:.72}
.bc-account-card h1{font-family:Georgia,serif;font-size:clamp(38px,6vw,68px);line-height:1.02;margin:10px 0 16px}
.bc-lead{font-size:18px;line-height:1.75;max-width:760px}
.bc-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:24px}
.bc-actions a{border:1px solid rgba(31,26,23,.16);border-radius:999px;padding:13px 18px;background:#fff;color:#1f1a17;text-decoration:none}
.bc-actions a:first-child{background:#1f1a17;color:#fff}
.bc-note{margin-top:24px;padding:18px;border-radius:20px;background:#fff;border:1px solid rgba(31,26,23,.12);line-height:1.65}
</style>
