export function classifyLumenIntent(text=""){ const c=String(text||"").toLowerCase(); if(c.includes("audit")||c.includes("scan")) return "RUN_AUDIT"; if(c.includes("expansion")) return "EXPANSION"; if(c.includes("website")) return "WEBSITE"; if(c.includes("status")||c.includes("operations")) return "STATUS"; return "GENERAL_LUMEN_REQUEST"; }
export function createLumenProposal({text="",actor="user"}={}){ return {id:`proposal_${Date.now()}`,createdAt:new Date().toISOString(),actor,originalText:text,intent:classifyLumenIntent(text),status:"proposal",requiresFounderApproval:false}; }
