import { useEffect, useState } from "react";
import { theme, NativeCard } from "../../lib/brianco-native/ui";

export default function FounderOperationsCenter(){

  const [data,setData] = useState(null);

  useEffect(()=>{
    fetch("/api/founder-ops/dashboard")
      .then(r=>r.json())
      .then(setData)
      .catch(()=>setData(null));
  },[]);

  const cards = data?.cards || [];

  return(
    <main style={theme.page}>
      <section style={theme.shell}>

        <h1 style={{
          fontSize:48,
          color:theme.gold,
          marginBottom:8
        }}>
          Brian & Co Founder Operations Center
        </h1>

        <p style={{
          fontSize:18,
          lineHeight:1.7,
          maxWidth:1100
        }}>
          Unified founder intelligence for memberships,
          chatbot systems, accessibility,
          localization, deployments,
          approvals, digital products,
          events and ecosystem operations.
        </p>

        <section style={{
          display:"grid",
          gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",
          gap:16,
          marginTop:24
        }}>
          {cards.map(card=>(
            <NativeCard
              key={card.title}
              title={`${card.icon} ${card.title}`}
              value={card.value}
              note="Founder Intelligence"
              trend={card.trend}
            />
          ))}
        </section>

        <section style={{
          display:"grid",
          gridTemplateColumns:"repeat(auto-fit,minmax(320px,1fr))",
          gap:16,
          marginTop:24
        }}>

          <article style={theme.card}>
            <h2 style={{color:theme.gold}}>
              Membership Operations
            </h2>

            <p>
              Concierge memberships,
              upgrades,
              bundles,
              subscriptions,
              renewals,
              and future recommendations.
            </p>
          </article>

          <article style={theme.card}>
            <h2 style={{color:theme.gold}}>
              Accessibility Operations
            </h2>

            <p>
              Accessibility adoption,
              accommodations,
              localization,
              language preferences,
              and future enhancement opportunities.
            </p>
          </article>

          <article style={theme.card}>
            <h2 style={{color:theme.gold}}>
              Deployment Operations
            </h2>

            <p>
              Shopify CLI,
              Vercel deployments,
              provider readiness,
              command center health,
              and system validation.
            </p>
          </article>

          <article style={theme.card}>
            <h2 style={{color:theme.gold}}>
              Event Operations
            </h2>

            <p>
              Cheyenne events,
              festivals,
              future vendor opportunities,
              and event intelligence.
            </p>
          </article>

        </section>

      </section>
    </main>
  );
}
