export default async function handler(req,res){res.status(200).json({ok:true,route:"storefront-feed",message:"Storefront feed route restored. Next expansion connects live current Lumen setup product cards."});}

